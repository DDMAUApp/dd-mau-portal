node_modules
dist
dist-snapshot/
.DS_Store
*.log
.env
.env.local
.env.*.local
.sentryclirc
toast-menu-snapshot.json
firebase-service-account.json
serviceAccountKey.json
gmail-oauth-client.json
backups/
# Vitest writes temp config files like vitest.config.js.timestamp-*.mjs
# during test runs that should clean up but sometimes leak.
vitest.config.js.timestamp-*
