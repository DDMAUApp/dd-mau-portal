[Service]
# Force HDMI full-range RGB right before lightdm takes the display —
# at boot the splash still owns it, so a standalone unit gets
# Permission denied. 2026-06-10, washed-out colors on the 4K panel.
ExecStartPre=/usr/local/bin/hdmi-full-range.sh
