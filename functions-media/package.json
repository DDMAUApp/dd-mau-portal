{
  "name": "functions-media",
  "description": "DD Mau — media processing Cloud Functions (chat video → phone-friendly MP4 + poster). Separate codebase so the ffmpeg binary never ships in the main functions image.",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "dependencies": {
    "ffmpeg-static": "^5.3.0",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^7.2.5"
  },
  "private": true
}
