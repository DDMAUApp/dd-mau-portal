<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!-- 2026-06-01 — Firebase Cloud Messaging default notification channel.
             Android 8+ requires every notification to belong to a channel.
             Without this default, FCM creates a generic "Miscellaneous"
             channel which lands push under a system-named bucket users
             can't recognize. Naming the channel explicitly lets us label
             it "DD Mau" in Settings → Notifications → DD Mau → Channels.
             Notification icon + color meta-data are intentionally omitted
             for the MVP — Android will use the app launcher icon, which
             is acceptable for first store submission. A polished
             notification-only silhouette icon can be added later as a
             quality-of-life improvement (referenced @drawable/ic_stat_dd_notification
             when ready). -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="dd_default_channel" />
    </application>

    <!-- Permissions — 2026-05-31, added during Capacitor wrap.
         POST_NOTIFICATIONS is mandatory on Android 13+ for FCM to ring.
         Without it pushes are silently dropped. Camera + microphone +
         location are runtime-prompted; the manifest entry is the floor
         that lets the runtime even ask. -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
</manifest>
